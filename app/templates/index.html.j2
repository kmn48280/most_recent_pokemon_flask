{% extends 'base.html.j2' %}

{% block title %}
Homepage 
{% endblock %}

{% block section %}
<section class="hero">
    <div class="hero-content">
       <h1 class="hero-title">
            Pokemon
       </h1>
       <h2 class="hero-subtitle">
            Welcome to my Poke Page!
       </h2>
       <button type="button" class="hero-button" onclick="location.href='{{url_for('main.pokedex')}}'">
            Gotta catch them all!
       </button>
    </div>
</section>
{% endblock %}

{% block content %}

{# {% if current_user.battles %}
{% for battle in current_user.battles %}
<table class="table">
    <thead>
      <tr>
        <th scope="col">Battles</th>
        <th scope="col">Wins</th>
        <th scope="col">Losses</th>
      </tr>
    </thead>
    <tbody>
      <tr class="table-primary">
        <th scope="row">{{current_user.id}} VS {{user.id}}</th>
        <td>{{current_user.wins}}</td>
        <td>{{current_user.losses}}</td>
      </tr>
    </tbody>
</table>
{% endfor %}
{% else %}
<span class="text">You have not battled anyone yet! Grab your Pokemon and battle!</span>

{% endif %} #}

{% endblock %}

