  <div class="top-social">
    <div class="container nav-container">
      <ul class="list-inline text-right">
        {# <li class="list-inline-icon"><img src={{user.get_icon()}} class="img-fluid rounded-start" alt="avatar"></li> #}
        <li class="list-inline-logout"><a href="{{url_for('auth.logout')}}"><i class="fa-solid fa-arrow-right-from-bracket"></i>
        <li class="list-inline-edit profile"><a href="{{url_for('auth.edit_profile')}}"><i class="fa-solid fa-square-user"></i></a></li>
      </ul>
    </div>
  </div>

  <!-- ========================= SECTION CONTENT ========================= -->

  <nav id="navbar" class="navbar navbar-expand-lg navbar-dark sticky-top bg-info">
    <div class="container nav-container">
        <a href="{{url_for('auth.login')}}"><img src="{{url_for('static', filename='/images/favicon.ico')}}" alt="" width="30" height="24" class="d-inline-block align-text-top"></a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#main_nav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="main_nav">
        <ul class="navbar-nav ml-auto">
          <li class="nav-item active" id="register-nav-ink"> <a class="nav-link" href="{{url_for('auth.register')}}">Register</a> </li>

{% if current_user.is_authenticated %}
          <li class="nav-item dropdown">
            <a class="nav-link  dropdown-toggle" href="{{url_for('main.index')}}" role="button" data-bs-toggle="dropdown">Profile</a>
            <ul class="dropdown-menu">
              <li><a class="dropdown-item" href="{{url_for('auth.login')}}">Login</a></li>
              <li><a class="dropdown-item" href="{{url_for('main.index')}}">Home</a></li>
              <li><a class="dropdown-item" href="{{url_for('auth.edit_profile')}}">Edit Profile</a></li>
              <li><a class="dropdown-item" href="{{url_for('auth.logout')}}">Log Out</a></li>
            </ul>
          </li>
          <li class="nav-item active"><a class="nav-link" href="{{url_for('main.pokedex')}}">Pokedex</a></li>
          <li class="nav-item active"><a class="nav-link" href="{{url_for('social.show_users')}}">Socialize</a></li>
{% endif %}
        </ul>
      </div> <!-- navbar-collapse.// -->
    </div><!-- container //  -->
  </nav>

